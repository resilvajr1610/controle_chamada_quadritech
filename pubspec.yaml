name: controle_chamada_quadritech
description: "Gerenciamento escolar"
publish_to: 'none'
version: 1.0.4+5

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  firebase_core:
  cloud_firestore:
  firebase_auth:
  brasil_fields:
  firebase_storage:
  multi_select_flutter: ^4.1.2

flutter:

  uses-material-design: true

  assets:
    - assets/imagens/


